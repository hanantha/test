# .bash_profile

# Get the aliases and functions
if [ -f ~/.bashrc ]; then
	. ~/.bashrc
fi

# User specific environment and startup programs

#alias mail="mail -r `whoami`\@centurysoftware.com.my" 

PATH=$PATH:$HOME/bin

export PATH
